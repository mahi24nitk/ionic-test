import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.galleryionictest1234.app',
  appName: 'photo-gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
